// import {useSelector} from 'react-redux';
// import {getCamerasTotalCount, getCurrentPage} from '../../store/app-process/selectors';
// import {getCameras, getCamerasFetchStatus} from '../../store/app-cameras/selectors';
// import {getPromos, getPromosFetchStatus} from '../../store/app-promos/selectors';

// const useCatalogSelectors = () => {

  // const promos = useSelector(getPromos);
  // const promosFetchStatus = useSelector(getPromosFetchStatus);
  //promos, promosFetchStatus,
  // return {cameras, camerasFetchStatus, currentPageNumber, camerasTotalCount};
// };

// export default useCatalogSelectors;

export {};
